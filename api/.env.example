# --- API core ---
PORT=3000
DATABASE_URL=postgresql://user:password@localhost:5432/seamai_db
# Local: no DATABASE_SSL. Remote Aiven (see .env.render.example):
#   DATABASE_URL=postgres://user:pass@host:port/db   (no ?sslmode= in URL)
#   DATABASE_SSL=true
#   DATABASE_SSL_CA_PATH=./api/dist/certs/ca.pem
# CA file: api/src/certs/ca.pem (copied to api/dist/certs on build). Do not use NODE_EXTRA_CA_CERTS.
# Max pg pool connections per process (default 5). Keep total across all pods ≤ DB max_connections.
# DATABASE_POOL_SIZE=5
JWT_SECRET=change-me-min-10-chars
TOKEN_ENCRYPTION_SECRET=change-me-min-16-chars

# Cloudinary (meeting audio + voice sample)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Redis (BullMQ: Jira/GitHub sync workers share this connection)
REDIS_HOST=localhost
REDIS_PORT=6379
# REDIS_PASSWORD=

# When `true`, BullMQ is not loaded: Jira/GitHub sync processors are no-ops (webhooks log errors).
# Meeting ai-engine callbacks are unaffected. Manual GitHub sync runs inline when queues are disabled.
# DISABLE_QUEUES=false
# Socket.IO multi-instance adapter (uses REDIS_*). Set `false` to disable; `force` to enable without queues.
# REALTIME_REDIS_ADAPTER=

# --- Meeting processing ---
# Shared secret for `POST /internal/meetings/:id/result` (ai-engine-2 or bridge posts results here).
# WORKER_SECRET=

# Required for meeting uploads: Nest forwards audio to ai-engine-2:
# POST {AI_ENGINE_BASE_URL}/api/v1/meetings/process-audio (form: meeting_id, project_id, file).
# The engine returns HTTP 202 quickly and processes in the background; results POST to /internal/meetings/:id/result.
# Max time (ms) for download + upload until 202 only (default 600000 = 10 min). Use the same WORKER_SECRET in Nest and ai-engine-2.
AI_ENGINE_BASE_URL=http://localhost:8000
# AI_ENGINE_REQUEST_TIMEOUT_MS=600000

# --- Google (user sign-in / sign-up) ---
# Create OAuth 2.0 credentials: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI must match GOOGLE_REDIRECT_URI (backend callback).
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
# GOOGLE_OAUTH_SUCCESS_REDIRECT_URL=http://localhost:8080/auth/google/success

# --- GitHub (OAuth + repository webhook) ---
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_REDIRECT_URI=http://localhost:3000/integrations/github/callback
# GITHUB_OAUTH_SUCCESS_REDIRECT_URL=http://localhost:5173/settings/integrations
# Webhook: POST {API_URL}/integrations/github/webhook — Content type application/json.
# Set the same value in GitHub → Repo → Webhooks → Secret. Header X-Hub-Signature-256 is verified.
# GITHUB_WEBHOOK_SECRET=

# --- CORS & Chrome extension (Phase 9) ---
# Comma-separated allowed browser origins (exact match, no trailing slash).
# FRONTEND_URL is also merged into this list automatically.
# Local + deployed dashboards:
# CORS_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,https://seam-ai-web-dashboard.onrender.com,https://seam-ai-2zkh6.ondigitalocean.app
# FRONTEND_URL=http://localhost:8080

# --- Auth cookies (cross-origin dashboard ↔ API) ---
# Local Vite proxy (same-origin): leave AUTH_COOKIE_CROSS_SITE unset → SameSite=Lax.
# Split hosts (DO / Render): AUTH_COOKIE_CROSS_SITE=true (API must be served over HTTPS).
# Or set NODE_ENV=production to default cross-site cookies on unless AUTH_COOKIE_CROSS_SITE=false.
AUTH_COOKIE_CROSS_SITE=true
# Optional shared parent domain: AUTH_COOKIE_DOMAIN=.yourdomain.com

# Max meeting audio upload (MB). Default 500. Align reverse-proxy body limits.
# MEETING_UPLOAD_MAX_MB=500

# --- Background scheduling (Phase 8) ---
# BullMQ repeatable GitHub sync-all interval in ms (default 600000 = 10 min).
# GITHUB_SYNC_REPEAT_MS=600000
